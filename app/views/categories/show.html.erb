<h1><%= @category.name %></h1>

<h3><%= pluralize(@category.posts.count, 'Post') %></h3>

<!-- Add code here to list a category's posts. -->
<ul>
<%@category.posts.each do |post_instance|%>
<li> <%= link_to post_instance.title, post_path(post_instance)%> </li>
<%end%>
</ul>
